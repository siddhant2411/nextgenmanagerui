import React from 'react';
import InventoryItem from '../components/inventoryitem';

const Inventory = () => {
    return (
        <div>
            <InventoryItem />
        </div>
    );
};

export default Inventory;
